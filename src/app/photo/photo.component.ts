import { Component, Input } from "@angular/core";
import { Subscriber } from "rxjs";

@Component({
    selector: 'app-st-photo',
    templateUrl: './photo.component.html',
})
export class PhotoComponent {
    @Input() url=""

    
        }
